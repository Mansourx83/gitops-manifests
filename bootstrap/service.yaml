apiVersion: v1
kind: Service
metadata:
  name: bootstrap-app-service
  namespace: jenkins
  labels:
    app: bootstrap-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: bootstrap-app
